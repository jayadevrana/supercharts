# SuperCharts — Build changelog (full history)

> Archived from `CLAUDE.md` → `## Recent log` on 2026-06-10. Newest first.
> CLAUDE.md keeps only the latest ~5 entries; when it grows past that, move the
> older entries here (verbatim, top of this list).

- 🧈 **Smooth auto-fit price scale — TV-style "axis follows the data" (chart-core).** New pure `price-fit.ts` (`fitTarget` / frame-rate-independent `smoothStep` exponential approach / `isNearRange` animate-vs-snap guard / `rangesOverlap`) + ChartCore `priceAutoFit` state machine: while auto (default) the price axis tracks the visible candles through pans, flings and live ticks via a smoothed (τ=100ms) retargeting fit advanced inside the existing rAF loop — no more candles drifting off-screen while panning history; backfill/snapshot refits morph instead of hard-cutting (order-of-magnitude jumps still snap). Manual control: vertical chart-drag past a 12px dead zone or a price-axis grab (TV semantics; wheel/resize then respect the pinned range; data swaps re-arm auto only if the pinned range would render blank); dblclick re-arms auto + eased re-fit; auto-mode flings are horizontal-only. Verified: 12/12 new `tests/price-fit.test.ts` (ran via clean vitest@2 in the Linux sandbox on verbatim file copies — repo vitest 4/rolldown native binding is macOS-only there; re-run the full suite on the Mac); `tsc --noEmit` exit 0 for chart-core + web; browser on /terminal (BTC/USDT 1m live): pan-back retargeted the axis 61400–62800 → 61200–62600 with candles filling the pane, vertical drag pinned the scale and live ticks respected it, dblclick reset+refit; 0 console errors across a reload+pan+dblclick cycle.
- 🎛️ **Script INPUT optimizer — MetaTrader EA-style optimization for coded strategies (8d36230).** `optimizeScript` (apps/api/src/script-optimizer.ts) sweeps a PulseScript's own `input.num` parameters: {from,step,to} per input (≤4 inputs, ≤1000 combos), re-runs the script per combination on the same real candles, backtests its marks, ranks with the SAME filters/metrics/objectives as the MA optimizer (helpers + `robustnessFlags` now exported from optimizer.ts; generic N-D ±1-step neighbour robustness; honest note + below-bar fallback). **Time-budgeted** (10s default) with a deterministic fixed-seed shuffle so truncation samples evenly — "evaluated N of M" reported, same inputs ⇒ same results. `POST /api/optimize/script` validates the script once (line-numbered 400) before sweeping. Pine dock gained an **Optimizer** tab (Console | Tester | Optimizer): Run discovers numeric inputs → sweep toggles + ranges + live combo count → objective/win-floor → ranked table → **Apply** writes values into the script inputs + re-runs on chart. Verified: 246/246 tests (6 new incl. strict determinism + truncation honesty), live 221-combo sweep 5.7s (#1 fastLen=12 slowLen=18 +27.16% robust), browser e2e 760-combo sweep with honest truncation + Apply; 0 console errors.
- 🏁 **MetaTrader-style optimizer in the Strategy Tester (87a1020).** `POST /api/optimize` — standalone sweep over the active chart (no alert needed): MetaTrader `{from,step,to}` ranges per parameter, default grid = **1,048 combinations** (fast 2–35×1, slow 5–110×3, cap 5000), each a REAL backtest of the last 1000 candles; reuses `runOptimizer` objective ranking + hard filters + robustness + realism pass-through verbatim. Strategy Tester dialog gained mode tabs **Single test | Optimizer · N combinations**: from/step/to inputs with live count, objective toggle, min-win-rate slider, ranked pass table ($ profit, win %, trades, DD, capped PF, robustness chip) and per-row **Use** (loads into Single test + plots BUY/SELL on chart). Honesty: on 1m ALL 1,048 combos fail PF≥1 and the sweep says so (accuracy objective shows the real negative returns) instead of crowning the least-bad loser. Verified live: 1h sweep 1,048 evaluated / 42ms, #1 EMA 13×17 +29.3% robust, Use plotted 214 crossovers; 236/236 tests.
- 🧪 **PulseScript Strategy Tester — code a strategy, backtest its marks (the TradingView model).** A script's `mark buy`/`mark sell` ARE the strategy: new `runSignalBacktest(candles, {index,side}[], interval, realism?)` in backtester.ts (trade walks generalized over a price source + shared summary builder; signal path ≡ MA-cross path, **pinned by exact-equivalence tests** with and without realism). New `POST /api/backtest/script` runs the user's script server-side (same sandbox: loop caps, 2s timeout) over the last 1000 real candles and trades its marks; line-numbered `script_error` 400s, `no_signals` explainer. Pine dock right column now tabs **Console | Strategy Tester** — Backtest button pushes the script to the chart (traded marks visible on real candles) and renders stat grid / equity sparkline / trade list with SL-TP exit chips / realism inputs. **Fail-loud fix in script-lang:** `input.num` with a non-numeric default (the title-first habit from other platforms) used to silently coerce to the min — live it turned EMA 9×21 into EMA 2×3 and produced a 259-trade garbage backtest; now a line-numbered RuntimeError. `docs/strategy-backtesting.md` = the full how-to. Verified: 236/236 tests (9 new incl. equivalence + footgun), typechecks clean, live API (footgun→400; correct script→39 trades +17.51% matching a local run), browser (sample backtested on 1000 real candles: 15 buy/16 sell marks, honest losing stats, marks on chart, 0 console errors). NOTE: `reserch/` is the owner's personal research — gitignored, never commit it.
- 🤖 **Multi-agent build: drawing UX root-cause fix + MT5 hardening + backtester realism + chart feel (4 commits, 227 tests green).** (1) **Drawing tools "don't work" — root cause found & fixed** (`009d639`): two-point tools finalized on the FIRST pointerup, so a TradingView-style click-move-click user got an invisible zero-length drawing + tool deselect. `drawing-controller.ts` now supports BOTH gestures (click <6px travel arms the draft → preview follows cursor → second click finalizes; ≥6px drag finalizes on release) + Escape cancels via new `cancelDraft()`. Lesson: verify with the gesture USERS use, not just the gesture robots use. (2) **MT5** (`c74af25`): CRITICAL — pairing tokens were never re-hydrated from SQLite after an API restart (tsx watch restarts on every edit → every EA pairing silently stranded). Now hydrated at boot + expired rows pruned; token 24h window renews on attach/detach; `mt5_accounts` table finally written (honest "awaiting EA reconnect" state across restarts); new `GET /api/mt5/status`; close-fraction validation (NaN never reaches the EA); connect dialog shows LIVE status (connected/awaiting/none/unreachable) + real bridge port + loopback warning; chip shows amber "MT5 · awaiting EA"; order-panel errors surfaced. 10 new tests; `docs/mt5-setup.md` (code-derived). KNOWN: ws-gateway broadcasts MT5 events unscoped per-user — must fix before multi-user auth. (3) **Backtester realism** (`6321bfb`): optional commissionPct / slippagePct / stopLossPct / takeProfitPct (worst-case SL-before-TP intrabar; trades carry `exitReason`). All default OFF — legacy output byte-identical (cmp-verified on live API). Threaded through /api/backtest, OptimizeRequest (pass-through, ranking untouched), and a "Realism" row in the Strategy Tester. 8 new hand-computed tests. Live: +19.92% → +13.27% with 0.05%/0.02% costs. (4) **Chart feel INC-18** (`b039b8c`): momentum pan (velocity-tracked, rAF exponential decay, killed on pointerdown/wheel, suppressed while drawing) + eased cursor-anchored wheel zoom (~120ms easeOutCubic, retargets on rapid wheel). Browser-verified incl. drawing-gesture regression. All verified by an independent 3-agent wave: 16/16 checks, 144 alerts untouched.
- 🧩 **PulseScript moved to a TradingView-style "Pine Editor" bottom dock.** Pine Script lives in a docked, resizable panel at the bottom of the chart in TradingView; SuperCharts' PulseScript was a cramped modal. New `pulse-editor-panel.tsx` is a docked panel at the bottom of the chart column (wired to the previously-unused `showBottomPanel` store flag): drag-to-resize top handle (180px–80vh), a "PulseScript" tab header + symbol/interval badge, Open/Save/Sample/On-chart/Run controls, the CodeMirror editor on the left (now **911px wide** vs the old 190px) and the console + Inputs panel on the right. The top-bar **Script** button now toggles it (accent when open) instead of opening a modal; the chart auto-shrinks to make room (ChartCore's ResizeObserver handles the reflow). Same `@supercharts/script-lang` engine + save/open/delete (`/api/scripts`) as before — pure relocation, no engine change. Retired `code-terminal-dialog.tsx`. **Verified:** web typecheck clean; full suite 209 green; headless browser — Script docks at the bottom (editor 911px, chart 899→559px), the sample script compiled + ran ("EMA Cross ran", plots on chart), 0 console errors.
- 🚑 **Demo-breaker triage — 5 client-demo issues fixed in one pass.** (1) **Systemic dialog clamp (root cause of the "cramped/awful" feel):** `cn` is plain clsx (no tailwind-merge), so the base `DialogContent`'s `max-w-lg` silently beat every override — *every* wide dialog (Alerts, Peak Performance, the PulseScript editor, Strategy Tester…) was stuck at 512px. Fixed in `components/ui/dialog.tsx`: only emit the default `max-w-lg` when the caller didn't pass its own `max-w-*`, so overrides win. Verified: PulseScript dialog 512→**1024px** (editor 190→**722px** — no longer "half hidden by inputs"); Peak Performance + Alerts un-cramped. Audited all DialogContent widths — only 3 `max-w-md` dialogs shift (512→448, their designed size); everything else widens or is unchanged. (2) **Strategy Tester / Backtest discoverability (#4):** new top-bar **Backtest** button → `backtest-dialog.tsx` runs a **real** backtest of the active chart's symbol/interval via a new `POST /api/backtest` (reuses the exact `runMaCrossBacktest` the alerts + optimizer use — real candles only). Shows stat grid (net return + $, win rate, trades, PF, Sharpe, max DD, avg win/loss), an equity curve, and a verifiable trade table (entry/exit time+price, bars, P&L); **"Plot BUY/SELL on chart"** pins the run onto the live candles (`backtestPreview` in the store → `MaCrossLayer`) so the client sees the exact entries — verified live (234 crosses rendered, EMA lines + green/red labels). (3) **"Permanent" BUY/SELL signals (#1):** added a **MA-cross signals** toggle in the Layers tab (`overlays.maSignals`, default on, persisted-undefined treated as on) so alert-driven signals can be turned off; backtest-plotted signals have their own Hide button; both already clear on symbol change. (4) **Drag + drawing (#2/#3):** proven WORKING via a real-CDP-input test (pan shifted the range; trend-line tool + drag created a drawing) — the live-demo failure was the documented USB-SSD-drop freezing the dev server. Hardened `chart-grid.tsx` so if `activePaneId` matches no pane it falls back to the first pane → exactly one pane always active, so `getTool`/drawing can never silently no-op. **Verified:** api + web typecheck clean; full suite **209 green**; headless browser confirmed all of the above with 0 console errors. The 144 live alerts + Telegram config were untouched (backtest is read-only compute).
- 🏆 **Peak Performance — "best settings" finder (on top of the real backtester).** Given an MA-cross alert, sweep every fast/slow MA pair, run the **real** `runMaCrossBacktest` per combo, then rank the *full qualifying pool* by the client's chosen objective — **💰 Profit / 🎯 Accuracy / ⚖️ Balanced** — with an adjustable **min win-rate floor**. The core (`apps/api/src/optimizer.ts` `rankPeak`, all helpers pure + unit-tested) applies HARD pre-filters so the flashy fluke is *removed, not down-ranked*: min-trades (default 10), the win-rate floor, must-have-a-losing-trade (excludes the suspicious zero-loss/∞-PF combos), positive expectancy + PF≥1 for profit/balanced, max-drawdown cap. Survivors are enriched with `expectancyPct`, **`profitFactorCapped` (Infinity→5, never leaks to the wire/comparator)**, a 0–1 `qualityScore` (Sharpe·expectancy·PF·hit-rate − DD penalty, scaled by sample-size confidence), and neighbour-based **robustness flags** (lone-spike / deep-DD / few-trades / low-PF / robust — *zero extra backtests*, computed from the grid it already ran). Sort is per-objective with **deterministic config-key tiebreaks** (Apply writes to a live alert, so order must be reproducible). The legacy optimizer path (no `objective`) is preserved byte-for-byte. UI: `OptimizerModal` is now self-fetching (objective 3-seg toggle · win-rate slider · account-size $ · top-10 table) — **$ is derived client-side from the real return % × account size, never stored or ranked**; an honest banner states no fees/slippage/leverage and that **Apply redraws that setting's actual BUY/SELL crossover labels on real candles so the client can verify the backtest before trusting capital**. **Verified:** 19 unit tests (`tests/optimizer-peak.test.ts`, incl. the named 9×75 +750%@15%-win fluke excluded by the floor for all 3 objectives) — full suite **209 green**; api + web typecheck clean; live API on a **real** BTC/USDT 30m alert (1000 real Binance bars, 29 combos) → the 3 objectives produce genuinely different rankings, 28/29 correctly dropped below a 50% floor, PF capped at 5.00; headless browser — the modal renders the toggle + slider + $ column + robustness chips with real numbers (balanced #1 EMA 20×50, +$1.7K/+17.2%/53%/robust), 0 console errors. The 144 live alerts + Telegram config were never mutated (optimize is read-only). **Do not guess / do not fake the backtest data — honoured: every number is a real backtest, $ is a transparent % conversion.**
- 📣 **Phase 4 · #17 — Telegram broadcast channel.** Push one-to-many trade signals/messages to a Telegram *channel* (where one of the user's bots is an admin) — distinct from the private alert chat. Strictly **additive**: it reads `telegram_bots` read-only and reuses the proven `sendTelegramMessage`; it never edits the alert path or bot config. Pure tested `normalizeChannelId` (`apps/api/src/telegram-broadcast.ts`: `@name` / bare name / `t.me/…` link / numeric id → canonical) + a new read-only `getTelegramChat` in `telegram.ts` that validates a channel via `getChat` **without sending anything**. `telegram_channels` + `telegram_broadcasts` (log) tables; `routes/broadcast.ts`: `GET/POST/DELETE /api/telegram/channels` (add validates with real getChat; stores only on success) + `POST /api/telegram/channels/:id/broadcast` (real send, **parseMode None** so arbitrary user text can't break Telegram's HTML parser; every attempt logged) + `GET …/broadcasts`. `BroadcastDialog` (top-bar **Broadcast**): bot picker + channel link form, channel list with unlink, a compose box with char count, and a recent-broadcast log; shows a "add a bot first" hint when none exist. **Verified:** 6 unit tests (`tests/telegram-broadcast.test.ts`); API smoke — 3 real bots listed, fake bot → 404, **a real bot + a non-existent channel hit live `getChat` read-only (Telegram replied "chat not found", nothing stored, no message sent)**, broadcast to a missing channel → 404; channels stayed empty and the 3 live bots were untouched; headless browser — the dialog renders with the bot dropdown + link form. No real broadcast was fired (no spam to subscribers); the live-send path reuses the already-proven sender. typechecks: api + web. **Next: Phase 4 · #18 (embedded iframe charts).**
- 🔗 **Phase 4 · #16 — Public strategy share links.** Publish a strategy (a SignalRecipe) to a read-only public page at `/s/<token>` — the first Phase 4 (collaboration) feature. The trust boundary is a pure, unit-tested sanitizer (`apps/api/src/strategy-share.ts`): it allow-lists only the strategy-describing fields (name, symbol, interval, logic, conditions, actions, indicatorSpecs, risk caps) and never copies `userId`, `accountId`, `enabled`, the recipe id, or an action filter's internal `recipeId`. `strategy_shares` table (token → sanitized snapshot, one per recipe, re-share refreshes it); `routes/share.ts` exposes authed `POST/GET/DELETE /api/signals/:id/share` (keyed by recipe, owner-scoped) and a **public, no-auth** `GET /api/public/strategy/:token` (safe precisely because the snapshot is already account-free). Web: `lib/strategy-describe.ts` turns the structured conditions/actions into plain English (`EMA(9) crosses above EMA(21)`, `Open BUY (market) · 1% risk · SL 150 pips · TP 300 pips`); the public page `app/s/[token]/page.tsx` is a branded, read-only card (SiteHeader/Footer, numbered entry conditions with ALL/ANY, action rows, risk caps, a "Start on SuperCharts" CTA, and an explicit "no account or broker details are included" note); a Share button in the strategy-builder Active list POSTs the share and copies the URL. **Verified:** 4 unit tests (`tests/strategy-share.test.ts`, incl. a leak-prevention assertion); API smoke (share → public read shows zero owner/account/recipe leakage, bad token → 404, revoke → link 404s); headless browser — the public `/s/<token>` page renders with no auth cookie, and the builder row shows the Copy-share-link button. typechecks: api + web. **Next: Phase 4 · #17 (Telegram broadcast channel).**
- 🪝 **Phase 3 · #15 — Inbound webhook receiver (Phase 3 COMPLETE).** External systems POST trade signals into SuperCharts over HTTP. Each user has one secret URL `/api/webhooks/in/<token>` (`nanoid(24)`, regeneratable). Parsing is a pure, unit-tested module (`apps/api/src/webhook-signal.ts`) that normalises a JSON **or** plain-text body into our own `{symbol, action, price, note, raw}` schema (generic aliases like ticker/side/message accepted; a non-JSON string becomes the note) plus an HTML-safe Telegram formatter — no third party's API is reproduced. `webhook_endpoints` + `webhook_events` tables; `routes/webhooks.ts` exposes the public token-authed receiver (registers `text/plain` + form parsers so an odd content type can't 415 before the token check; 404 on an unknown token; trims to the last 200 events) and authed manage/regenerate/forward-toggle/clear. **Telegram forward is opt-in and OFF by default**, and reuses the user's existing enabled bot **read-only** — it never touches the alerts/Telegram config, so test posts stay silent. `WebhooksDialog` (top-bar **Webhook**): secret URL + copy, regenerate, forward switch, an example payload, and a live recent-signals list with colour-coded buy/sell badges. **Verified:** 10 unit tests (`tests/webhook-signal.test.ts`); API smoke (JSON + text/plain + form-encoded all stored, bad token → 404, events cleared after); headless browser — dialog shows the URL + three posted signals. typechecks: api + web. **Phase 3 is now done; next: Phase 4 · #16 (public strategy share links).**
- 📄 **Phase 3 · #14 — CSV import for custom OHLC.** Users upload their own OHLC candles and chart them as a `CUSTOM:<slug>` symbol. Parsing is a pure, unit-tested module (`apps/api/src/csv-ohlc.ts`): auto-detects delimiter (`,`/`;`/tab), an optional header (alias map → columns) vs positional `time,o,h,l,c[,v]`, and the timestamp format (UNIX s/ms by magnitude, or `Date.parse` for ISO/date strings); infers the native bar interval from the median row spacing; drops invalid rows with warnings (never invents data); caps at 5000 rows. `custom_datasets` table + `routes/custom-data.ts` (POST parse+store+seed the live candle store, GET list, DELETE) — re-seeded into the candle store at boot. Serving needs **no chart changes**: an unknown `CUSTOM:` venue resolves to no provider, so `/api/candles` returns cache-only; added a small branch there to return all stored rows regardless of the requested `from/to` window so historical uploads chart. `ImportCsvDialog` (top-bar **Import**): file picker (read client-side), name, warnings, a list of saved datasets with delete, and one-click "open on the active pane" (sets symbol + native interval). chart-pane skips the WS subscribe for static `CUSTOM:` symbols. **Bug fixed (surfaced by this):** `ChartCore.setCandles` fit the price scale *before* the time scale was rescaled for the new bar duration, so static data (no live ticks to refit) stuck at a tiny price band — reordered to fit after, which also tightens every timeframe switch. **Verified:** 11 unit tests (`tests/csv-ohlc.test.ts`); end-to-end smoke round-tripping 420 **real** BTC daily candles (parsed → stored → served identically); headless browser — Import dialog lists the dataset, clicking it charts the CUSTOM symbol with a correctly fit price scale (60k–84k). typechecks: types + chart-core + api + web. **Next: Phase 3 · #15 (TradingView webhook receiver).**
- 📅 **Phase 3 · #13 — Economic calendar overlay.** Macro events drawn as vertical markers on the chart. Real, keyless source (the published Forex Factory weekly JSON mirror) fetched in `routes/calendar.ts` (`GET /api/calendar/economic`, 30-min cache, impact filter, and an honest `status:'unavailable'` on a feed failure — never fabricated; last-good cache preferred). Parsing lives in a pure, unit-tested `apps/api/src/economic-calendar.ts` (`normalizeEconomicEvents`: ISO→UTC-ms, impact mapping, currency upper-casing, drops invalid rows, stable ids). New chart-core `EconomicEventsLayer` (zIndex 11) renders an impact-coloured dashed vertical line per event (high=red, medium=amber), a currency tag at the time axis, and a crosshair-hover tooltip with title + forecast/previous. Wired as a per-pane Layers-tab toggle (`overlays.economicEvents`), fetched once and shared across panes. Added a public `ChartCore.invalidate()` so toggling an overlay repaints immediately rather than waiting for the next tick — and fixed an overlay-survival bug: the effect now re-applies on symbol/interval change (the ChartCore is rebuilt on those, so a toggle-only dep silently dropped the overlay after a timeframe switch). **Verified:** 7 unit tests (`tests/economic-calendar.test.ts`); endpoint smoke (115 real events, 13 high / 11 medium, impact filter); headless browser on /terminal at 1h — red/amber vertical markers + USD/EUR tags landing on the real 06-01/06-02 events (ISM PMI, Powell, EUR CPI). typechecks: types + chart-core + api + web. **Next: Phase 3 · #14 (CSV import for custom OHLC).**
- 📰 **Phase 3 · #12 — News filter per watchlist.** New original engine `apps/api/src/news-relevance.ts` turns a watchlist's symbols into factual keyword sets (coin names, currency names, central banks, metals, index names, explicit pair forms like `EURUSD`/`EUR/USD`), each weighted by confidence. It (a) biases the upstream fetch — `buildGdeltQuery` for the keyless GDELT doc API + `cryptoCurrenciesFor` for CryptoPanic's `currencies` — and (b) re-ranks whatever returns via `scoreItem`, computing a real 0–1 relevance locally (no upstream "relevance" trusted) and reporting exactly which symbols each headline touches. `GET /api/news/watchlist/:id` (auth + 404 on unknown list, empty when the list has no symbols). News tab gained scope chips (active symbol vs each saved list); watchlist rows show a relevance bar + matched-symbol badges. **Verified:** 14 unit tests (`tests/news-relevance.test.ts`) covering keyword mapping, whole-word matching, ranking, GDELT query build, currency extraction; endpoint smoke (404/empty/shape); headless browser — News tab → Default chip active → per-watchlist fetch fires. NOTE: at capture time GDELT's public endpoint was rate-limiting our shared IP (the *existing* news feature was equally empty), so the browser showed the honest empty state rather than populated rows — no data was fabricated. Follow-up worth noting: surface `NewsResult.status:'rate_limited'` from `fetchGdelt` so the empty state can distinguish "throttled upstream" from "no matches", and a paid news key (CryptoPanic/Finnhub) would make the feed reliable. **Next: Phase 3 · #13 (economic calendar overlay).**
- 🔌 **Phase 3 · #11 — OANDA onboarding wizard.** Top-bar **OANDA** button → `OandaConnectDialog`: enter API token + account id + practice/live, "Validate & connect" hits `POST /api/oanda` which checks the creds against the **real** OANDA account-summary endpoint (`oanda_credentials` table; token server-side only, surfaces OANDA's own error on failure, stores nothing unless it 200s). On boot the API reads the saved row and uses it for the live forex feed (overriding env, else Yahoo). Verified: dummy token → real OANDA rejection, browser flow. **Next: Phase 3 · #12 (news filter per watchlist).** Follow-up worth noting: newly-saved creds only take effect on the next server start (boot-time read) — a hot-swap of the running OANDA provider would make it instant.
- 🧬 **PulseScript COMPLETE (Phase 6, tasks 1–8).** Task 8 = the safety pass: `RunOptions.timeoutMs` (2s default; per-bar + every-4096-step wall-clock abort) + `maxBars` (50k) on top of the loop-step cap; verified scripts can't reach host globals/IO; `ta.*` periods clamp to ≥1 so `sma(close, 0)` floors to a 1-bar window. 56 script-lang tests green. The full language now spans lexer → parser → interpreter → stdlib (reusing `@supercharts/indicators`) → inputs → in-app CodeMirror terminal (`draw`/`mark` on a dedicated `pulse-script` layer) → save/list/load (`user_scripts` table) → sandbox. **Next active: Phase 3 · #11 — OANDA token onboarding wizard.**
- 🔭 **Order-flow + futures set shipped:** real footprint pipeline (`apps/ingestion/src/footprint-aggregator.ts` → WS → `FootprintLayer`), Time & Sales tape, DOM ladder, Open Interest (`routes/futures.ts`, Binance USD-M, 30s cache). Plus RVOL, VWAP σ-bands, Initial Balance, Naked POC, Market Profile/TPO (`MarketProfileLayer`).
- 📸 Alerts ship a rendered crossover PNG to Telegram (`alert-chart.ts`); cold-start false-alert flood fixed (backfill + watermark).
