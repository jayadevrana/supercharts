# Screenshots

This folder contains public screenshots used by the repository README.

The current set is captured from a local development run:

- `terminal-overview.png`
- `indicator-browser.png`
- `pulsescript-editor.png`
- `strategy-tester.png`
- `alerts-dialog.png`
- `pricing.png`

Before publishing new screenshots, make sure they do not show private provider credentials, account IDs, broker balances, Telegram chat IDs, webhook URLs, or local tunnel URLs.
